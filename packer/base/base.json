{
    "builders": [
        {
            "type": "digitalocean",
            "api_token": "{{API_KEY}}",
            "image": "ubuntu-12-04-x64",
            "region": "lon1",
            "size": "512mb"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "script": "provision.sh"
        }
    ]
}
